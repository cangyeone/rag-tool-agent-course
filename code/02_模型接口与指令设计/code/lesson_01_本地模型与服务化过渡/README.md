# 本地模型与服务化过渡

## 主要讲什么

- 这一组脚本把本地模型调用和在线 API 调用放在一起比较，展示请求、响应、错误处理和密钥读取的基本结构。

## 基本概念

- REST API：通过 HTTP 请求调用模型。
- 环境变量：在代码外保存 API Key 等配置。
- 请求头：包含认证和内容类型等信息。
- 响应体：模型服务返回的 JSON 数据。
- 错误兜底：接口失败时给出可读提示。
