# 输入输出与对话格式


## 对应课程大纲

| 时间 | 主题 | 课程要点 | 对应代码 |
| --- | --- | --- | --- |
| 08:40-09:10 | 课程导入：为什么需要大模型 | 信息处理问题、自然语言交互、知识问答、文档理解、流程辅助 | 输入输出、messages 结构、边界检查 |
| 09:10-09:45 | 传统机器学习发展历史 | 规则系统、专家系统、统计学习、特征工程、常见机器学习模型 | 大模型与小模型历史沿革、字符进入模型前的形态 |
| 09:45-10:10 | 深度学习模型发展历史 | 神经网络、CNN、RNN/LSTM、Attention、Transformer、预训练模型 | tokenizer、next token 循环、本地 Qwen 检查与逐步输出 |

本目录代码用于配合上午上半场课程材料，重点让学员先看到输入、token、对话格式、流式输出和本地小模型生成过程。

所属半天：6月22日上午_大模型基础与DeepSeek接入
课次：第 01 节

## 本节课目标

本节课按 6 个 demo 展开。每个 demo 都能单独运行，适合先讲 3-5 分钟，再可改一个参数观察输出。

## 演示脚本

### Demo 1: 大模型与小模型历史沿革

脚本：`01_大模型与小模型历史沿革.py`

演示重点：用时间线解释统计语言模型、小模型、预训练大模型和应用助手的演进。

运行方式：

```bash
python 01_大模型与小模型历史沿革.py
```

### Demo 2: 字符进入模型前长什么样

脚本：`02_字符进入模型前长什么样.py`

演示重点：从一串中文问题开始，观察字符、词片段和简单 token id。

运行方式：

```bash
python 02_字符进入模型前长什么样.py
```

### Demo 3: messages 对话结构

脚本：`03_messages_对话结构.py`

演示重点：把 system、user、assistant 三种角色拆开观察。

运行方式：

```bash
python 03_messages_对话结构.py
```

### Demo 3: 原始文本续写

脚本：`03_原始文本续写.py`

演示重点：去掉 Chat 封装，用最原始的方式使用 Qwen——给它一段文本看它如何续写，并观察逐字符输入时 token 的动态变化。

运行方式：

```bash
python 03_原始文本续写.py
```

### Demo 4: 基于Qwen模型的流式输入

脚本：`04_流式输入.py`

演示重点：使用 TextStreamer 和手动逐 token 生成，体验流式输出的效果和内部机制。

运行方式：

```bash
python 04_流式输入.py
```

### Demo 5: Chat模式 —— 原始拼接

脚本：`05_chat原始拼接.py`

演示重点：不依赖模板函数，手动使用 Qwen 的 ChatML 标记（<|im_start|>/<|im_end|>）拼接对话格式，理解底层原理。

运行方式：

```bash
python 05_chat原始拼接.py
```

### Demo 6: Chat模式 —— 使用模板函数

脚本：`06_chat模板函数.py`

演示重点：掌握 `apply_chat_template()` 的用法，对比手动拼接理解模板函数的便利性。

运行方式：

```bash
python 06_chat模板函数.py
```

### Demo 7: think 标签和清理

脚本：`07_think标签清理.py`

演示重点：用 Qwen 生成带 think 标签的输出，识别并清理不同模型的思考标签变体（<think>、<thinking>、\`\`\`think 等）。

运行方式：

```bash
python 07_think标签清理.py
```

### Demo 8: 其他开源模型的 Chat 模板格式

脚本：`08_其他模型chat模板.py`

演示重点：对比 Qwen、DeepSeek、Llama、Mistral 的 chat 模板差异，理解为什么不能混用模板。

运行方式：

```bash
python 08_其他模型chat模板.py
```

### Demo 9: 生成参数调参体验

脚本：`09_生成参数调参.py`

演示重点：理解 temperature、do_sample、top_k、top_p、repetition_penalty、max_new_tokens 的含义，通过同一问题不同参数的对比实验，观察输出变化。

运行方式：

```bash
python 09_生成参数调参.py
```

### Demo 10: 多模态输入 —— 图像消息格式

脚本：`10_输入图像的例子.py`

演示重点：展示多模态模型的 messages 格式——content 从字符串变为列表，image 和 text 如何一起传入；对比纯文本模型与多模态模型在输入格式上的核心区别。

运行方式：

```bash
python 10_输入图像的例子.py
```

### Demo 11: DeepSeek API 调用完整示例

脚本：`11_DeepSeek_API调用.py`

演示重点：从构造请求体、配置全部参数，到发送同步/流式请求、解析响应、提取 token 用量和 finish_reason，以及 deepseek-v4-flash 与 deepseek-v4-pro 的选型对比。

运行方式：

```bash
python 11_DeepSeek_API调用.py
```

## 推荐节奏

- Demo 1：建立本节课概念入口。
- Demo 2-3：字符与 token 的关系，原始文本续写体验，展开核心代码，可改输入或参数。
- Demo 4：流式输入体验，理解 token 逐个生成的过程。
- Demo 5-6：Chat 模式从底层手动拼接到高层模板函数，理解 messages 到模型输入的完整链路。
- Demo 7：think 标签清理，理解思考过程与最终回答的分离。
- Demo 8：对比不同模型的 chat 模板格式，理解跨模型适配。
- Demo 9：生成参数调参体验，理解 temperature/do_sample/top_k/top_p 对输出的影响。
- Demo 10：多模态输入，理解 content 从字符串到列表的变化，image + text 的组合方式。
- Demo 11：DeepSeek API 调用，理解请求体结构、参数含义、同步/流式、think 标签处理。

## 统一修改点

- 改用户问题，观察检索、路由或质检输出是否变化。
- 改规则资料，观察命中片段和回答依据是否变化。
- 改工具参数，观察结构化结果和最终回答是否变化。
- 把终端输出发给课堂助教，让它解释这个 demo 在知识地图中的位置。

## 本节课产出

- 能说清本节课 11 个 demo 的先后关系。
- 至少完成 1 次参数修改，并解释输出为什么变化。
- 记录 1 个可以带到下一节课的问题。

## 下一节衔接

下一节进入：本地模型与服务化过渡。
本节课最后建议留 3 分钟，可把当前 demo 的输出和下一节的输入对应起来。