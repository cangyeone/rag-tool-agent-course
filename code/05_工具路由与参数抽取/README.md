# 05 工具路由与参数抽取

## 主要讲什么

- 这一章讲工具数量变多以后如何选择工具。重点是先用规则、关键词和向量检索缩小候选范围，再把少量工具 schema 交给模型判断。

## 基本概念

- Router：根据任务类型选择处理路径。
- 规则过滤：用明确关键词先定位工具组。
- 向量召回：根据工具描述找语义相关工具。
- LLM rerank：让模型判断候选工具是否真正相关。
- Schema 注入：只给最终少量工具的完整参数定义。
- 参数抽取：从用户问题中提取工具所需字段。
