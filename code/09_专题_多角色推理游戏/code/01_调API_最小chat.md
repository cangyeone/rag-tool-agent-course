# 01 调API 最小chat

## 给 OpenCode 的任务说明

请根据下面的说明阅读、实现或修改脚本：`code/09_专题_多角色推理游戏/code/01_调API_最小chat.py`。

这个脚本的主题是：**01 调API 最小chat**。
现有代码开头提示：01 调 API：最小 chat 示例。 课堂目标： 1. 看懂 chat messages 的基本形状。

## 目标

- 用尽量简单的 Python 代码演示一个清楚的小知识点。
- 代码适合初学者顺序阅读，少隐藏细节。
- 关键步骤写中文注释。
- 如果需要在线模型，必须从环境变量读取 API Key，不允许硬编码。

## 输入

- 默认使用脚本内置的通用示例问题或示例文档。
- 学员可以修改示例文本、检索问题、模型参数或工具描述。

## 输出

运行脚本后，终端应该打印关键中间过程和最终结果。
如果依赖缺失或没有配置 API Key，脚本应该给出可读提示。

## 建议实现步骤

1. 打印脚本标题，让学员知道正在运行哪一节。
2. 准备一组最小示例数据。
3. 按步骤处理数据，不要过早封装复杂函数。
4. 打印每一步的结果。
5. 在结尾给出“可以修改尝试的点”。

## 可以让 OpenCode 做的修改

```text
请根据这个 Markdown 任务说明，重写同名 Python 脚本。
要求：代码更适合初学者，步骤清楚，中文注释详细，不要硬编码 API Key。
```

```text
请给这个脚本增加 3 个可以修改的参数，并在运行输出中解释这些参数的作用。
```

```text
请把脚本里的示例数据换成我自己的公开文档场景，并保持整体结构不变。
```

## 学习检查

- 我能说出脚本输入是什么。
- 我能说出脚本中间做了哪些处理。
- 我能修改一个参数并解释输出为什么变化。