# 本地模型与生成原理

## 主要讲什么

- 这一组脚本补充本地模型调用、逐步生成、HTTP 服务封装和模型后端切换，帮助理解从本地推理到服务化调用的过渡。

## 基本概念

- Tokenizer：把文本切分并映射成 token id。
- Next token：语言模型每一步预测的下一个 token。
- 本地模型：在本机加载权重并推理。
- HTTP 服务：把模型能力包装成可请求的接口。
- 模型后端：本地模型、Ollama、在线 API 等不同调用来源。
