# 04 关键词和向量一起找工具

这个脚本用一个简化版本说明“混合召回”。

真实项目里可以用 BM25 和 Embedding。这里先用普通 Python 模拟：

- 关键词分数：问题里的词是否出现在工具说明里。
- 相似度分数：问题和工具说明的词集合有多少重叠。
- 最终分数：两者相加。

可以试着改：

- 改 `query`。
- 改工具说明。
- 把 `set_similarity` 换成真实向量相似度。
