# 05 只把少量 schema 给模型

这个脚本讲 schema 注入。

思路很简单：

1. 完整工具定义放在本地。
2. Router 先筛出少量候选工具。
3. 只把候选工具的完整参数说明给模型。

这样做有两个好处：

- 上下文更短。
- 模型不容易在一堆无关工具里选错。

可以试着改：

- 改 `selected`。
- 增加一个工具 schema。
- 只保留前 3 个候选工具。
